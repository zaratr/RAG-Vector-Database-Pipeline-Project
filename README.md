# RAG + Vector Database Pipeline -> GraphRAG

Upgraded in 2026 to utilize **GraphRAG**. Standard semantic search is augmented with a Knowledge Graph built using NetworkX and local LLM entity extraction (Ollama + Gemma).
